import RatingFeedback from './RatingFeedback';
import { renderWithTheme } from 'utils/test-utils';

describe('Component: RatingFeedback', () => {
  it('should render correctly', () => {
    renderWithTheme(<RatingFeedback />);
  });
});
